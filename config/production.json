{
  "binance": {
    "apiKey": "${BINANCE_API_KEY}",
    "apiSecret": "${BINANCE_API_SECRET}"
  },
  "twitter": {
    "enabled": "${TWITTER_ENABLED:false}",
    "appKey": "${TWITTER_APP_KEY}",
    "appSecret": "${TWITTER_APP_SECRET}",
    "accessToken": "${TWITTER_ACCESS_TOKEN}",
    "accessSecret": "${TWITTER_ACCESS_SECRET}",
    "minGoldScore": "${TWITTER_MIN_GOLD_SCORE:60}",
    "retryAttempts": "${TWITTER_RETRY_ATTEMPTS:3}",
    "retryDelay": "${TWITTER_RETRY_DELAY:1000}"
  },
  "telegram": {
    "enabled": "${TELEGRAM_ENABLED:false}",
    "botToken": "${TELEGRAM_BOT_TOKEN}",
    "chatId": "${TELEGRAM_CHAT_ID}"
  },
  "showdoc": {
    "enabled": "${SHOWDOC_ENABLED:false}",
    "pushUrl": "${SHOWDOC_PUSH_URL}"
  },
  "email": {
    "enabled": "${EMAIL_ENABLED:false}",
    "smtp": {
      "host": "${EMAIL_SMTP_HOST}",
      "port": "${EMAIL_SMTP_PORT:587}",
      "secure": "${EMAIL_SMTP_SECURE:false}",
      "auth": {
        "user": "${EMAIL_SMTP_USER}",
        "pass": "${EMAIL_SMTP_PASS}"
      }
    },
    "from": "${EMAIL_FROM}",
    "to": "${EMAIL_TO}"
  },
  "monitoring": {
    "port": "${MONITORING_PORT:5010}"
  },
  "logging": {
    "level": "${LOG_LEVEL:info}"
  }
}